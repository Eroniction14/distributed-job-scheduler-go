apiVersion: v1
kind: ConfigMap
metadata:
  name: scheduler-config
data:
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_USER: postgres
  DB_PASSWORD: postgres
  DB_NAME: job_scheduler
  KAFKA_BROKER: kafka:9092